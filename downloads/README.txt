Place the release APK here as: FunctionPlane.apk

The site links to /downloads/FunctionPlane.apk from:
- the hero "Download APK" button
- the "Download APK" card in the #download section

Both use the HTML `download` attribute, so clicking starts the file
download immediately instead of navigating away.
